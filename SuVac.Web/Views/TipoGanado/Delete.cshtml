@model SuVac.Application.DTOs.TipoGanadoDTO
@{
    ViewData["Title"] = "Eliminar Tipo de Ganado";
}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1 data-i18n="tipoGanado.eliminarTitle">Eliminar Tipo de Ganado</h1>
            <div class="alert alert-warning" data-i18n="tipoGanado.confirmarEliminar">¿Está seguro?</div>
            <div class="card mb-3">
                <div class="card-body">
                    <p><strong>ID:</strong> @Model.TipoGanadoId</p>
                    <p><strong data-i18n="comun.nombre">Nombre</strong>: @Model.Nombre</p>
                </div>
            </div>
            <form asp-action="DeleteConfirmed" asp-route-id="@Model.TipoGanadoId" method="post">
                <button type="submit" class="btn btn-danger" data-i18n="comun.eliminar">Eliminar</button>
                <a asp-action="Index" class="btn btn-secondary" data-i18n="comun.cancelar">Cancelar</a>
            </form>
        </div>
    </div>
</div>