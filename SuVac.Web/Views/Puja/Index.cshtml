@model IEnumerable<SuVac.Application.DTOs.PujaDTO>

@{
    ViewData["Title"] = "Pujas";
    var total = Model?.Count() ?? 0;
}

<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2 mb-3">
    <div>
        <h1 class="h3 mb-1" data-i18n="puja.historialTitle">Historial de Pujas</h1>
        <p class="text-muted mb-0"><span data-i18n="puja.totalPujas">Total de pujas:</span> <strong>@total</strong>.</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i> <span data-i18n="puja.nuevaPuja">Nueva Puja</span>
    </a>
</div>

<div class="card shadow-sm border-0">
    <div class="card-header bg-body d-flex align-items-center justify-content-between">
        <span class="fw-semibold" data-i18n="puja.listado">Listado</span>
        <span class="small text-muted"><span data-i18n="puja.mostrando">Mostrando</span> <strong>@total</strong> <span data-i18n="puja.registros">registro(s)</span></span>
    </div>

    <div class="card-body p-0">
        @if (Model == null || !Model.Any())
        {
            <div class="p-4 text-center">
                <div class="display-6 mb-2"><i class="bi bi-cash-coin"></i></div>
                <h2 class="h5 mb-2" data-i18n="puja.noHay">No hay pujas registradas</h2>
                <p class="text-muted mb-0" data-i18n="puja.noHayDesc">No se encontraron pujas en el sistema.</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light position-sticky top-0" style="z-index:1;">
                        <tr>
                            <th class="ps-3" style="width:60px;">#</th>
                            <th data-i18n="puja.colSubasta">Subasta</th>
                            <th data-i18n="puja.colUsuario">Usuario</th>
                            <th style="width:130px;" data-i18n="puja.colMonto">Monto</th>
                            <th style="width:170px;" data-i18n="puja.colFecha">Fecha y Hora</th>
                            <th class="text-end pe-3" style="width:170px;" data-i18n="puja.colAcciones">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-3 text-muted small">@item.PujaId</td>
                                <td>
                                    <a asp-controller="Subasta" asp-action="Detalle" asp-route-id="@item.SubastaId" asp-route-from="pujas"
                                           class="text-decoration-none">
                                        Subasta #@item.SubastaId
                                    </a>
                                </td>
                                <td>@item.NombreUsuario</td>
                                <td class="fw-semibold">₡@item.Monto.ToString("N2")</td>
                                <td class="text-muted small">@item.FechaHora.ToString("dd/MM/yyyy HH:mm")</td>
                                <td class="text-end pe-3">
                                    <a asp-action="Details" asp-route-id="@item.PujaId"
                                           class="btn btn-sm btn-outline-secondary">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.PujaId"                            class="btn btn-sm btn-outline-warning">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.PujaId"                     class="btn btn-sm btn-outline-danger">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>