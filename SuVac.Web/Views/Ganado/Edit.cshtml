@model SuVac.Application.DTOs.GanadoDTO
@using SuVac.Application.DTOs

@{
    ViewData["Title"] = "Editar Ganado";
    var categorias = ViewBag.Categorias as ICollection<CategoriaDTO> ?? new List<CategoriaDTO>();
    var categoriasSeleccionadas = ViewBag.CategoriasSeleccionadas as List<int> ?? new List<int>();
    var imagenes = Model.ImagenesGanado?.ToList() ?? new List<ImagenGanadoDTO>();
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h3 mb-1"><i class="bi bi-pencil-square me-2"></i>Editar Ganado</h1>
        <p class="text-muted mb-0">ID: <strong>#@Model.GanadoId</strong> — @Model.Nombre</p>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i> Volver al Catálogo
    </a>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="GanadoId" />
            <input type="hidden" asp-for="UsuarioVendedorId" />
            <input type="hidden" asp-for="FechaRegistro" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <h5 class="text-secondary mb-3 border-bottom pb-2">Información General</h5>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="Nombre" class="form-label fw-semibold">Nombre <span class="text-danger">*</span></label>
                    <input asp-for="Nombre" class="form-control" />
                    <span asp-validation-for="Nombre" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="PesoKg" class="form-label fw-semibold">Peso (kg) <span class="text-danger">*</span></label>
                    <input asp-for="PesoKg" class="form-control" type="number" step="0.01" min="1" max="9999" />
                    <span asp-validation-for="PesoKg" class="text-danger small"></span>
                </div>
            </div>
            <div class="mb-3">
                <label asp-for="Descripcion" class="form-label fw-semibold">Descripción <span class="text-danger">*</span> <small class="text-muted">(mínimo 20 caracteres)</small></label>
                <textarea asp-for="Descripcion" class="form-control" rows="4"></textarea>
                <span asp-validation-for="Descripcion" class="text-danger small"></span>
            </div>
            <div class="row">
                <div class="col-md-4 mb-3">
                    <label asp-for="TipoGanadoId" class="form-label fw-semibold">Tipo de Ganado <span class="text-danger">*</span></label>
                    <select asp-for="TipoGanadoId" asp-items="@ViewBag.TiposGanado" class="form-select">
                        <option value="">-- Seleccionar Tipo --</option>
                    </select>
                    <span asp-validation-for="TipoGanadoId" class="text-danger small"></span>
                </div>
                <div class="col-md-4 mb-3">
                    <label asp-for="RazaId" class="form-label fw-semibold">Raza <span class="text-danger">*</span></label>
                    <select asp-for="RazaId" asp-items="@ViewBag.Razas" class="form-select">
                        <option value="">-- Seleccionar Raza --</option>
                    </select>
                    <span asp-validation-for="RazaId" class="text-danger small"></span>
                </div>
                <div class="col-md-4 mb-3">
                    <label asp-for="SexoId" class="form-label fw-semibold">Sexo <span class="text-danger">*</span></label>
                    <select asp-for="SexoId" asp-items="@ViewBag.Sexos" class="form-select">
                        <option value="">-- Seleccionar Sexo --</option>
                    </select>
                    <span asp-validation-for="SexoId" class="text-danger small"></span>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="FechaNacimiento" class="form-label fw-semibold">Fecha de Nacimiento <span class="text-danger">*</span></label>
                    <input asp-for="FechaNacimiento" class="form-control" type="date"
                           min="@DateTime.Today.AddYears(-50).ToString("yyyy-MM-dd")"
                           max="@DateTime.Today.ToString("yyyy-MM-dd")" />
                    <span asp-validation-for="FechaNacimiento" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="CertificadoSalud" class="form-label fw-semibold">Condición / Certificado de Salud</label>
                    <input asp-for="CertificadoSalud" class="form-control" placeholder="Ej: CERT-1234-24" />
                    <span asp-validation-for="CertificadoSalud" class="text-danger small"></span>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="EstadoGanadoId" class="form-label fw-semibold">Estado</label>
                    <select asp-for="EstadoGanadoId" asp-items="@ViewBag.EstadosGanado" class="form-select">
                        <option value="">-- Seleccionar Estado --</option>
                    </select>
                    <span asp-validation-for="EstadoGanadoId" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label fw-semibold text-muted">Fecha de Registro <small class="text-danger">(no editable)</small></label>
                    <p class="form-control-plaintext ps-1">
                        <i class="bi bi-calendar me-1 text-muted"></i>@Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")
                    </p>
                </div>
            </div>

            
            <div class="mb-4 p-3 bg-light rounded border">
                <label class="form-label fw-semibold mb-1">Usuario Vendedor <span class="text-danger small">(no editable)</span></label>
                <div class="d-flex align-items-center gap-2">
                    <i class="bi bi-person-check-fill text-primary fs-5"></i>
                    <span class="fw-semibold">@(Model.NombreVendedor ?? $"ID: {Model.UsuarioVendedorId}")</span>
                    <small class="text-muted">— asignado al crear el objeto</small>
                </div>
            </div>

            
            <div class="mb-4">
                <label class="form-label fw-semibold">Categorías <span class="text-danger">*</span> <small class="text-muted">(seleccione al menos una)</small></label>
                <span asp-validation-for="CategoriasIds" class="text-danger small d-block mb-1"></span>
                @if (!categorias.Any())
                {
                    <div class="alert alert-warning py-2">No hay categorías disponibles.</div>
                }
                else
                {
                    <div class="row g-2">
                        @foreach (var cat in categorias)
                        {
                            <div class="col-md-4 col-sm-6">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox"
                                           name="CategoriasIds" value="@cat.CategoriaId"
                                           id="cat_@cat.CategoriaId"
                                           @(categoriasSeleccionadas.Contains(cat.CategoriaId) ? "checked" : "") />
                                    <label class="form-check-label" for="cat_@cat.CategoriaId">
                                        @cat.Nombre
                                    </label>
                                </div>
                            </div>
                        }
                    </div>
                }
            </div>

            
            <div class="mb-3">
                <label class="form-label fw-semibold">Imágenes <span class="text-danger">*</span></label>
                <span class="text-danger small d-block mb-1">
                    @(ViewData.ModelState["imagenesNuevas"]?.Errors.FirstOrDefault()?.ErrorMessage)
                </span>

                @if (imagenes.Any())
                {
                    <p class="text-muted small mb-2">Imágenes actuales (haga clic en × para eliminar):</p>
                    <div id="existentes-container" class="d-flex flex-wrap gap-3 mb-3">
                        @foreach (var img in imagenes)
                        {
                            <div class="position-relative imagen-existente">
                                <img src="@img.UrlImagen" alt="Imagen"
                                     class="rounded border" style="width:100px;height:80px;object-fit:cover;">
                                <input type="hidden" name="imagenesExistentes" value="@img.UrlImagen" />
                                <button type="button"
                                        class="btn btn-danger position-absolute top-0 end-0 p-0 lh-1"
                                        style="width:20px;height:20px;font-size:11px;"
                                        onclick="removeExistente(this)" title="Eliminar imagen">
                                    <i class="bi bi-x"></i>
                                </button>
                            </div>
                        }
                    </div>
                }

                <label class="form-label small fw-semibold text-muted">Agregar nuevas imágenes desde su equipo:</label>
                <div class="border rounded p-3 bg-light">
                    <div id="preview-nuevas" class="d-flex flex-wrap gap-2 mb-3"></div>
                    <label class="btn btn-outline-secondary mb-0" for="imagenesNuevas">
                        <i class="bi bi-image me-1"></i> Agregar imágenes
                        <input type="file" id="imagenesNuevas" name="imagenesNuevas" multiple
                               accept="image/jpeg,image/png,image/gif,image/webp" class="d-none" />
                    </label>
                    <small class="text-muted ms-2">JPG, PNG, GIF, WEBP · máx. 5 MB c/u</small>
                </div>
            </div>

            <hr class="my-3" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-warning">
                    <i class="bi bi-floppy me-1"></i> Guardar Cambios
                </button>
                <a asp-action="Details" asp-route-id="@Model.GanadoId" class="btn btn-outline-primary">
                    <i class="bi bi-eye me-1"></i> Ver Detalles
                </a>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        (() => {
            const input = document.getElementById('imagenesNuevas');
            const preview = document.getElementById('preview-nuevas');
            let allFiles = new DataTransfer();

            input.addEventListener('change', function () {
                Array.from(this.files).forEach(file => {
                    if (!file.type.startsWith('image/')) return;
                    allFiles.items.add(file);
                });
                renderPreviews();
                // NO limpiar input.value — borra input.files en Chrome
            });

            // Re-asignar los archivos acumulados justo antes de enviar
            document.querySelector('form[action*="Edit"]')?.addEventListener('submit', function () {
                input.files = allFiles.files;
            });

            function renderPreviews() {
                preview.innerHTML = '';
                Array.from(allFiles.files).forEach((file, idx) => {
                    const reader = new FileReader();
                    reader.onload = e => {
                        const div = document.createElement('div');
                        div.className = 'position-relative';
                        div.innerHTML = `
                            <img src="${e.target.result}" class="rounded border"
                                style="width:100px;height:80px;object-fit:cover;" title="${file.name}">
                            <button type="button" class="btn btn-danger btn-sm position-absolute top-0 end-0 p-0 lh-1"
                                style="width:1.2rem;height:1.2rem;font-size:.7rem;" data-idx="${idx}" title="Quitar">
                                &times;
                            </button>`;
                        div.querySelector('button').addEventListener('click', function () {
                            const i = parseInt(this.dataset.idx);
                            const dt = new DataTransfer();
                            Array.from(allFiles.files).forEach((f, j) => { if (j !== i) dt.items.add(f); });
                            allFiles = dt;
                            renderPreviews();
                        });
                        preview.appendChild(div);
                    };
                    reader.readAsDataURL(file);
                });
            }
        })();

        // Eliminar imagen existente (quita el hidden input y el thumb)
        function removeExistente(btn) {
            btn.closest('.imagen-existente').remove();
        }
    </script>
}