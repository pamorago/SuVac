@model IEnumerable<SuVac.Application.DTOs.SubastaListadoDTO>
@{
    ViewData["Title"] = "Subastas Finalizadas";
    var total = Model?.Count() ?? 0;
}

<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2 mb-3">
    <div>
        <h1 class="h3 mb-1" data-i18n="subasta.finalizadasTitle">Subastas Finalizadas</h1>
        <p class="text-muted mb-0"><span data-i18n="subasta.finalizadasSubtitulo">Historial de subastas cerradas. Total:</span> <strong>@total</strong>.</p>
    </div>
    <div class="d-flex gap-2">
        <a asp-action="Activas" class="btn btn-outline-success">
            <i class="bi bi-arrow-left me-1"></i> <span data-i18n="subasta.verActivas">Ver Activas</span>
        </a>
        <a asp-action="Create" asp-route-from="finalizadas" class="btn btn-primary">
            <i class="bi bi-plus-circle me-1"></i> <span data-i18n="subasta.nuevaSubasta">Nueva Subasta</span>
        </a>
    </div>
</div>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info d-flex align-items-center" role="alert">
        <i class="bi bi-info-circle me-2"></i>
        <div data-i18n="subasta.noHayFinalizadas">No hay subastas finalizadas en este momento.</div>
    </div>
}
else
{
    <section aria-label="Listado de subastas finalizadas">
        <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4">
            @foreach (var subasta in Model)
            {
                <div class="col">
                    <article class="card h-100 shadow-sm border-0">
                        <div class="rounded-top overflow-hidden bg-body-tertiary" style="height: 200px;">
                            @if (!string.IsNullOrEmpty(subasta.ImagenGanado))
                            {
                                <img class="w-100 h-100 object-fit-cover rounded-top"
                                     src="@subasta.ImagenGanado"
                                     alt="Imagen de @subasta.NombreGanado"
                                     loading="lazy"
                                     onerror="this.src='/img/sin-imagen.png'" />
                            }
                            else
                            {
                                <div class="d-flex flex-column justify-content-center align-items-center text-muted h-100">
                                    <i class="bi bi-image fs-1"></i>
                                    <span class="small" data-i18n="subasta.sinImagen">Sin imagen</span>
                                </div>
                            }
                        </div>

                        <div class="card-body">
                            <h2 class="h6 fw-semibold mb-2">@subasta.NombreGanado</h2>

                            <div class="d-flex flex-wrap gap-2 align-items-center mb-3">
                                @{
                                    var badgeClass = subasta.EstadoSubasta == "Finalizada" ? "text-bg-secondary" : "text-bg-danger";
                                }
                                <span class="badge @badgeClass">
                                    <i class="bi bi-circle-fill me-1"></i>@subasta.EstadoSubasta
                                </span>
                                <span class="badge text-bg-primary">
                                    <i class="bi bi-hammer me-1"></i>@subasta.CantidadPujas <span data-i18n="subasta.pujas">puja(s)</span>
                                </span>
                            </div>

                            <dl class="row small mb-0">
                                <dt class="col-5 text-muted" data-i18n="subasta.cierre">Cierre</dt>
                                <dd class="col-7">@subasta.FechaFin.ToString("dd/MM/yyyy HH:mm")</dd>
                            </dl>
                        </div>

                        <div class="card-footer bg-body border-0 pt-0 pb-3">
                            <div class="d-grid gap-2 px-2">
                                <a asp-action="Detalle" asp-route-id="@subasta.SubastaId" asp-route-from="finalizadas"
                                   class="btn btn-secondary">
                                    <i class="bi bi-eye me-1"></i> <span data-i18n="subasta.verDetalle">Ver detalle</span>
                                </a>
                                <a asp-action="HistorialPujas" asp-route-id="@subasta.SubastaId"
                                   class="btn btn-outline-primary">
                                    <i class="bi bi-list-ul me-1"></i> <span data-i18n="subasta.historialPujas">Historial de pujas</span>
                                </a>
                                <div class="d-flex gap-2">
                                    <a asp-action="Edit" asp-route-id="@subasta.SubastaId" asp-route-from="finalizadas"
                                       class="btn btn-outline-warning flex-fill">
                                        <i class="bi bi-pencil me-1"></i> <span data-i18n="subasta.editar">Editar</span>
                                    </a>
                                    <a asp-action="Cancelar" asp-route-id="@subasta.SubastaId"
                                       class="btn btn-outline-danger flex-fill">
                                        <i class="bi bi-x-circle me-1"></i> <span data-i18n="subasta.cancelar">Cancelar</span>
                                    </a>
                                </div>
                                @if (subasta.CantidadPujas > 0)
                                {
                                    <a asp-controller="Pago" asp-action="Index"
                                       class="btn btn-outline-success">
                                        <i class="bi bi-credit-card-2-front me-1"></i> Ver Pago
                                    </a>
                                }
                            </div>
                        </div>
                    </article>
                </div>
            }
        </div>
    </section>
}

@section Scripts {
    <style>
        .card:hover { transform: translateY(-3px); transition: transform .18s ease; }
    </style>
}

