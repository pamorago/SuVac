@model ICollection<SuVac.Application.DTOs.RazaDTO>

@{
    ViewData["Title"] = "Razas";
}

<div class="container mt-4">
    <div class="row mb-3">
        <div class="col-md-6">
            <h1 data-i18n="raza.title">Razas</h1>
        </div>
        <div class="col-md-6 text-end">
            <a asp-action="Create" class="btn btn-primary" data-i18n="raza.crearBtn">Crear Nueva Raza</a>
        </div>
    </div>

    @if (Model != null && Model.Count > 0)
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th data-i18n="comun.nombre">Nombre</th>
                    <th data-i18n="comun.descripcion">Descripción</th>
                    <th data-i18n="comun.acciones">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.RazaId</td>
                        <td>@item.Nombre</td>
                        <td>@item.Descripcion</td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.RazaId" class="btn btn-info btn-sm"
                               data-i18n="comun.verDetalles">Detalles</a>
                            <a asp-action="Edit" asp-route-id="@item.RazaId" class="btn btn-warning btn-sm"
                               data-i18n="comun.editar">Editar</a>
                            <a asp-action="Delete" asp-route-id="@item.RazaId" class="btn btn-danger btn-sm"
                               data-i18n="comun.eliminar">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info" role="alert" data-i18n="raza.noHay">
            No hay razas registradas.
        </div>
    }
</div>